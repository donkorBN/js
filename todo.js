const todoList = []

let inputValue = input.value

function addTodo(){
    const inputElement = document.querySelector('.js-button');
    const taskName = String(document.querySelector('.input_field'))
    const task = taskName.va
    todoList.push(taskName)
    console.log(todoList)
    }
